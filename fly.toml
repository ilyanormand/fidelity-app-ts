# fly.toml app configuration file generated for fidelity-app-ts on 2026-01-05T10:51:24+01:00
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'fidelity-app-ts'
primary_region = 'fra'

[build]

[deploy]
  rrelease_command = "npx prisma db push --force-reset --accept-data-loss"
  seed_command = 'npx tsx prisma/seed.ts'

[env]
  PORT = '3000'
  SCOPES = 'write_products'
  SHOPIFY_API_KEY = '6ca88552a33182eec3ac2453f573578c'
  SHOPIFY_APP_URL = 'https://fidelity-app-ts.fly.dev'

[http_service]
  internal_port = 3000
  force_https = true
  auto_stop_machines = 'stop'
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpus = 4
  memory_mb = 1024
