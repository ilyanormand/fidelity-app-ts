1. Page load
   ↓
2. Initialize reward items cache
   ↓
3. Lock UI "+" buttons
   ↓
4. Setup MutationObserver (tracking DOM changes)
   ↓
5. Intercept clicks on "+" buttons
   ↓
6. Intercept fetch/XHR requests to /cart/change and /cart/update
   ↓
7. When attempting to increase quantity:
   - Check: is the item a reward item?
   - If YES → block + alert
   - Replace quantity with current quantity
   - Send request with current quantity